[
  {
    "path": "bright_memory_trainer/__init__.py",
    "content": "\"\"\"Bright Memory Trainer - A memory training companion for Bright Memory.\"\"\"\n\n__version__ = \"1.0.0\"\n"
  },
  {
    "path": "bright_memory_trainer/trainer.py",
    "content": "\"\"\"Core trainer module for Bright Memory Trainer.\n\nProvides memory training exercises inspired by the game Bright Memory,\nincluding pattern recall, sequence memory, and reaction time challenges.\n\"\"\"\n\nimport random\nimport time\nfrom typing import List, Tuple, Optional\n\n\nclass MemoryExercise:\n    \"\"\"Base class for memory training exercises.\"\"\"\n\n    def __init__(self, difficulty: int = 1):\n        \"\"\"Initialize exercise with difficulty level (1-5).\"\"\"\n        self.difficulty = max(1, min(5, difficulty))\n        self.score = 0\n        self.attempts = 0\n\n    def get_complexity(self) -> int:\n        \"\"\"Return exercise complexity based on difficulty.\"\"\"\n        return self.difficulty * 2 + random.randint(0, 3)\n\n\nclass PatternRecall(MemoryExercise):\n    \"\"\"Exercise: Recall a sequence of light patterns (like Bright Memory's puzzles).\"\"\"\n\n    def __init__(self, difficulty: int = 1):\n        super().__init__(difficulty)\n        self.pattern_length = self.get_complexity()\n\n    def generate_pattern(self) -> List[str]:\n        \"\"\"Generate a random pattern of colors.\"\"\"\n        colors = [\"red\", \"blue\", \"green\", \"yellow\", \"purple\"]\n        return [random.choice(colors) for _ in range(self.pattern_length)]\n\n    def evaluate_recall(self, original: List[str], recalled: List[str]) -> float:\n        \"\"\"Evaluate recall accuracy as a percentage.\"\"\"\n        if not original or not recalled:\n            return 0.0\n        correct = sum(1 for o, r in zip(original, recalled) if o == r)\n        accuracy = (correct / len(original)) * 100\n        self.attempts += 1\n        self.score = max(self.score, accuracy)\n        return accuracy\n\n\nclass SequenceMemory(MemoryExercise):\n    \"\"\"Exercise: Memorize and repeat an increasing sequence of positions.\"\"\"\n\n    def __init__(self, difficulty: int = 1):\n        super().__init__(difficulty)\n        self.sequence: List[Tuple[int, int]] = []\n\n    def generate_sequence(self, length: int) -> List[Tuple[int, int]]:\n        \"\"\"Generate a grid position sequence (x, y) in a 4x4 grid.\"\"\"\n        self.sequence = []\n        for _ in range(length):\n            x = random.randint(0, 3)\n            y = random.randint(0, 3)\n            self.sequence.append((x, y))\n        return self.sequence\n\n    def check_sequence(self, user_sequence: List[Tuple[int, int]]) -> bool:\n        \"\"\"Check if user sequence matches the generated sequence.\"\"\"\n        self.attempts += 1\n        if user_sequence == self.sequence:\n            self.score += 100\n            return True\n        return False\n\n\nclass ReactionChallenge(MemoryExercise):\n    \"\"\"Exercise: React quickly to visual cues (like Bright Memory combat).\"\"\"\n\n    def __init__(self, difficulty: int = 1):\n        super().__init__(difficulty)\n        self.reaction_times: List[float] = []\n\n    def simulate_challenge(self, target_delay: float = 1.0) -> float:\n        \"\"\"Simulate a reaction time challenge and return the reaction time.\"\"\"\n        # Simulate a variable reaction time based on difficulty\n        base_reaction = random.uniform(0.2, 0.8)\n        reaction_time = base_reaction + (self.difficulty * 0.1)\n        time.sleep(min(reaction_time, 0.5))  # Prevent actual long waits\n        self.reaction_times.append(reaction_time)\n        self.attempts += 1\n        return reaction_time\n\n    def average_reaction_time(self) -> float:\n        \"\"\"Calculate average reaction time across attempts.\"\"\"\n        if not self.reaction_times:\n            return 0.0\n        return sum(self.reaction_times) / len(self.reaction_times)\n\n\ndef run_training_session(exercise_type: str = \"pattern\", difficulty: int = 1) -> dict:\n    \"\"\"Run a complete training session and return results.\n\n    Args:\n        exercise_type: Type of exercise (\"pattern\", \"sequence\", \"reaction\")\n        difficulty: Difficulty level (1-5)\n\n    Returns:\n        dict with session results\n    \"\"\"\n    if exercise_type == \"pattern\":\n        exercise = PatternRecall(difficulty)\n        pattern = exercise.generate_pattern()\n        # Simulate a perfect recall for demo purposes\n        accuracy = exercise.evaluate_recall(pattern, pattern)\n        return {\n            \"type\": \"PatternRecall\",\n            \"difficulty\": difficulty,\n            \"pattern_length\": len(pattern),\n            \"accuracy\": accuracy,\n            \"score\": exercise.score\n        }\n    elif exercise_type == \"sequence\":\n        exercise = SequenceMemory(difficulty)\n        seq = exercise.generate_sequence(exercise.get_complexity())\n        success = exercise.check_sequence(seq)\n        return {\n            \"type\": \"SequenceMemory\",\n            \"difficulty\": difficulty,\n            \"sequence_length\": len(seq),\n            \"success\": success,\n            \"score\": exercise.score\n        }\n    elif exercise_type == \"reaction\":\n        exercise = ReactionChallenge(difficulty)\n        for _ in range(3):\n            exercise.simulate_challenge()\n        avg_time = exercise.average_reaction_time()\n        return {\n            \"type\": \"ReactionChallenge\",\n            \"difficulty\": difficulty,\n            \"average_reaction_time\": round(avg_time, 3),\n            \"attempts\": exercise.attempts\n        }\n    else:\n        raise ValueError(f\"Unknown exercise type: {exercise_type}\")\n"
  },
  {
    "path": "bright_memory_trainer/cli.py",
    "content": "\"\"\"Command-line interface for Bright Memory Trainer.\"\"\"\n\nimport argparse\nimport sys\nfrom typing import List\nfrom .trainer import run_training_session\n\n\ndef parse_args(args: List[str]) -> argparse.Namespace:\n    \"\"\"Parse command-line arguments.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Bright Memory Trainer - Improve your memory with exercises inspired by Bright Memory.\"\n    )\n    parser.add_argument(\n        \"-t\", \"--type\",\n        choices=[\"pattern\", \"sequence\", \"reaction\"],\n        default=\"pattern\",\n        help=\"Type of memory exercise (default: pattern)\"\n    )\n    parser.add_argument(\n        \"-d\", \"--difficulty\",\n        type=int,\n        choices=range(1, 6),\n        default=1,\n        help=\"Difficulty level from 1 (easy) to 5 (hard) (default: 1)\"\n    )\n    parser.add_argument(\n        \"-v\", \"--version\",\n        action=\"store_true\",\n        help=\"Show version information\"\n    )\n    return parser.parse_args(args)\n\n\ndef main() -> None:\n    \"\"\"Main entry point for CLI.\"\"\"\n    args = parse_args(sys.argv[1:])\n\n    if args.version:\n        from . import __version__\n        print(f\"Bright Memory Trainer v{__version__}\")\n        sys.exit(0)\n\n    try:\n        result = run_training_session(args.type, args.difficulty)\n        print(\"\\n=== Training Session Results ===\")\n        for key, value in result.items():\n            print(f\"{key.replace('_', ' ').title()}: {value}\")\n        print(\"================================\\n\")\n    except ValueError as e:\n        print(f\"Error: {e}\", file=sys.stderr)\n        sys.exit(1)\n    except Exception as e:\n        print(f\"Unexpected error: {e}\", file=sys.stderr)\n        sys.exit(2)\n\n\nif __name__ == \"__main__\":\n    main()\n"
  },
  {
    "path": "tests/__init__.py",
    "content": "\"\"\"Test package for Bright Memory Trainer.\"\"\"\n"
  },
  {
    "path": "tests/test_trainer.py",
    "content": "\"\"\"Unit tests for the trainer module.\"\"\"\n\nimport unittest\nfrom bright_memory_trainer.trainer import (\n    PatternRecall,\n    SequenceMemory,\n    ReactionChallenge,\n    run_training_session\n)\n\n\nclass TestPatternRecall(unittest.TestCase):\n    \"\"\"Tests for PatternRecall exercise.\"\"\"\n\n    def setUp(self):\n        self.exercise = PatternRecall(difficulty=2)\n\n    def test_generate_pattern_length(self):\n        \"\"\"Test that pattern length matches complexity.\"\"\"\n        pattern = self.exercise.generate_pattern()\n        self.assertEqual(len(pattern), self.exercise.pattern_length)\n\n    def test_generate_pattern_colors(self):\n        \"\"\"Test that pattern contains valid colors.\"\"\"\n        valid_colors = {\"red\", \"blue\", \"green\", \"yellow\", \"purple\"}\n        pattern = self.exercise.generate_pattern()\n        for color in pattern:\n            self.assertIn(color, valid_colors)\n\n    def test_evaluate_recall_perfect(self):\n        \"\"\"Test perfect recall returns 100%.\"\"\"\n        pattern = self.exercise.generate_pattern()\n        accuracy = self.exercise.evaluate_recall(pattern, pattern)\n        self.assertEqual(accuracy, 100.0)\n\n    def test_evaluate_recall_partial(self):\n        \"\"\"Test partial recall returns correct percentage.\"\"\"\n        original = [\"red\", \"blue\", \"green\"]\n        recalled = [\"red\", \"yellow\", \"green\"]\n        accuracy = self.exercise.evaluate_recall(original, recalled)\n        self.assertAlmostEqual(accuracy, 66.666, places=2)\n\n    def test_evaluate_recall_empty(self):\n        \"\"\"Test empty recall returns 0%.\"\"\"\n        accuracy = self.exercise.evaluate_recall([\"red\"], [])\n        self.assertEqual(accuracy, 0.0)\n\n\nclass TestSequenceMemory(unittest.TestCase):\n    \"\"\"Tests for SequenceMemory exercise.\"\"\"\n\n    def setUp(self):\n        self.exercise = SequenceMemory(difficulty=1)\n\n    def test_generate_sequence_length(self):\n        \"\"\"Test sequence length matches complexity.\"\"\"\n        seq = self.exercise.generate_sequence(5)\n        self.assertEqual(len(seq), 5)\n\n    def test_generate_sequence_positions(self):\n        \"\"\"Test positions are within grid bounds.\"\"\"\n        seq = self.exercise.generate_sequence(10)\n        for x, y in seq:\n            self.assertGreaterEqual(x, 0)\n            self.assertLessEqual(x, 3)\n            self.assertGreaterEqual(y, 0)\n            self.assertLessEqual(y, 3)\n\n    def test_check_sequence_correct(self):\n        \"\"\"Test correct sequence returns True.\"\"\"\n        self.exercise.generate_sequence(3)\n        result = self.exercise.check_sequence(self.exercise.sequence)\n        self.assertTrue(result)\n\n    def test_check_sequence_wrong(self):\n        \"\"\"Test incorrect sequence returns False.\"\"\"\n        self.exercise.generate_sequence(3)\n        wrong_seq = [(0, 0), (0, 0), (0, 0)]\n        result = self.exercise.check_sequence(wrong_seq)\n        self.assertFalse(result)\n\n\nclass TestReactionChallenge(unittest.TestCase):\n    \"\"\"Tests for ReactionChallenge exercise.\"\"\"\n\n    def setUp(self):\n        self.exercise = ReactionChallenge(difficulty=3)\n\n    def test_simulate_challenge_returns_float(self):\n        \"\"\"Test reaction time is a float.\"\"\"\n        reaction = self.exercise.simulate_challenge()\n        self.assertIsInstance(reaction, float)\n\n    def test_simulate_challenge_positive(self):\n        \"\"\"Test reaction time is positive.\"\"\"\n        reaction = self.exercise.simulate_challenge()\n        self.assertGreater(reaction, 0)\n\n    def test_average_reaction_time_multiple(self):\n        \"\"\"Test average reaction time after multiple challenges.\"\"\"\n        for _ in range(5):\n            self.exercise.simulate_challenge()\n        avg = self.exercise.average_reaction_time()\n        self.assertGreater(avg, 0)\n        self.assertLess(avg, 2.0)  # Reasonable upper bound\n\n    def test_average_reaction_time_no_data(self):\n        \"\"\"Test average returns 0 when no data.\"\"\"\n        avg = self.exercise.average_reaction_time()\n        self.assertEqual(avg, 0.0)\n\n\nclass TestRunTrainingSession(unittest.TestCase):\n    \"\"\"Tests for run_training_session function.\"\"\"\n\n    def test_pattern_session(self):\n        \"\"\"Test pattern training session returns expected keys.\"\"\"\n        result = run_training_session(\"pattern\", 2)\n        self.assertIn(\"type\", result)\n        self.assertIn(\"accuracy\", result)\n        self.assertEqual(result[\"type\"], \"PatternRecall\")\n\n    def test_sequence_session(self):\n        \"\"\"Test sequence training session returns expected keys.\"\"\"\n        result = run_training_session(\"sequence\", 3)\n        self.assertIn(\"type\", result)\n        self.assertIn(\"success\", result)\n        self.assertEqual(result[\"type\"], \"SequenceMemory\")\n\n    def test_reaction_session(self):\n        \"\"\"Test reaction training session returns expected keys.\"\"\"\n        result = run_training_session(\"reaction\", 1)\n        self.assertIn(\"type\", result)\n        self.assertIn(\"average_reaction_time\", result)\n        self.assertEqual(result[\"type\"], \"ReactionChallenge\")\n\n    def test_invalid_exercise_type(self):\n        \"\"\"Test invalid exercise type raises ValueError.\"\"\"\n        with self.assertRaises(ValueError):\n            run_training_session(\"invalid\", 1)\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n"
  },
  {
    "path": "requirements.txt",
    "content": "# Bright Memory Trainer dependencies\n# No external dependencies required for core functionality\n# Add any future dependencies below\n"
  },
  {
    "path": "setup.py",
    "content": "\"\"\"Setup script for Bright Memory Trainer.\"\"\"\n\nfrom setuptools import setup, find_packages\n\nwith open(\"README.md\", \"r\", encoding=\"utf-8\") as fh:\n    long_description = fh.read()\n\nsetup(\n    name=\"bright-memory-trainer\",\n    version=\"1.0.0\",\n    author=\"Bright Memory Trainer Team\",\n    author_email=\"trainer@brightmemory.example.com\",\n    description=\"A memory training companion for Bright Memory\",\n    long_description=long_description,\n    long_description_content_type=\"text/markdown\",\n    url=\"https://github.com/example/bright-memory-trainer\",\n    packages=find_packages(),\n    classifiers=[\n        \"Programming Language :: Python :: 3\",\n        \"License :: OSI Approved :: MIT License\",\n        \"Operating System :: OS Independent\",\n        \"Development Status :: 4 - Beta\",\n        \"Intended Audience :: End Users/Desktop\",\n        \"Topic :: Games/Entertainment\",\n    ],\n    python_requires=\">=3.8\",\n    entry_points={\n        \"console_scripts\": [\n            \"bright-memory-trainer=bright_memory_trainer.cli:main\",\n        ],\n    },\n)\n"
  },
  {
    "path": "README.md",
    "content": "# Bright Memory Trainer\n\nA memory training companion for the game **Bright Memory**, featuring exercises inspired by in-game puzzles and combat mechanics.\n\n## Features\n\n- **Pattern Recall**: Memorize and reproduce color sequences (like Bright Memory's light puzzles)\n- **Sequence Memory**: Remember increasing sequences of grid positions\n- **Reaction Challenge**: Train your reflexes with timed responses\n- Adjustable difficulty from 1 (easy) to 5 (hard)\n- Command-line interface for quick sessions\n\n## Installation\n\n```bash\npip install -r requirements.txt\npip install .\n```\n\n## Usage\n\nRun a training session from the command line:\n\n```bash\nbright-memory-t
